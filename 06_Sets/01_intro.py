# Sets 
a = {1, 3, 54, 8, 84}
print(a, type(a))
b = set() # Empty set
print(b, type(b))

# It is a collection of well defined objects.

# It is unordered. 
# It cannot be indexed.
# There is no way to change items in sets.
# Elements in a set is not repeated.
# for example:
s = {1, 7, 8, 84, 97, 8, 84}
print(s)